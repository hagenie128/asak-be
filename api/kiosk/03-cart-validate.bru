meta {
  name: Kiosk Cart Validate
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/kiosk/cart/validate
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "items": [
      {
        "menuId": {{menuId}},
        "quantity": 1,
        "selectedOptionItemIds": [269],
        "excludedIngredientIds": []
      }
    ]
  }
}

docs {
  # API-004
  POST /api/kiosk/cart/validate
  CART_API_AND_DATA_CONTRACT.md
  요청 필드: selectedOptionItemIds, excludedIngredientIds (optionItems 아님)
}
