meta {
  name: Kiosk Categories
  type: http
  seq: 0
}

get {
  url: {{baseUrl}}/api/kiosk/categories
  body: none
  auth: none
}

docs {
  # API-001
  GET /api/kiosk/categories
  DB: category (sort_order, active)
}
