meta {
  name: Kiosk Categories
  type: http
  seq: 0
}

get {
  url: {{baseUrl}}/api/kiosk/categories
  body: none
  auth: none
}

docs {
  # API-001
  GET /api/kiosk/categories
  응답 code: OK (ApiResponse.success(data) 기본값)
  data: [{ categoryId, categoryName, sortOrder, isActive }]
}
