meta {
  name: Kiosk Menu Detail
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/kiosk/menuDetail/{{menuId}}
  body: none
  auth: none
}

docs {
  # API-003
  GET /api/kiosk/menuDetail/{menuId}
  응답: { success: true, status: 200, code: "SUCCESS", message: "OK", data: {...} }
  data: menuId, categoryId, name, price, imageUrl, description, isSoldOut, baseKcal, ingredients, optionGroups, tags
  ingredients[]: ingredientId, ingName, role, unit, isDefault, canRemove
  optionGroups[]: optionGroupId, name, groupType, selectType, minSelect, maxSelect, sortOrder, isRequired, items[]
  optionGroups[].items[]: optionItemId, ingredientId, name, extraPrice, originalPrice, servingAmount, servingUnit, isRecommended, isDefault, isSoldOut, kcal, protein
}
