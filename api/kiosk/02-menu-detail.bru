meta {
  name: Kiosk Menu Detail
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/kiosk/menuDetail/{{menuId}}
  body: none
  auth: none
}

docs {
  # API-003
  GET /api/kiosk/menuDetail/{menuId}
  응답 code: OK (ApiResponse.success(data) 기본값)
  data: menuId, categoryId, name, price, imageUrl, description, isSoldOut, ingredients, optionGroups, tags
}
