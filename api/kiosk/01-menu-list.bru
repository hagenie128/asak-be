meta {
  name: Kiosk Menu List
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/kiosk/menuList?categoryId={{categoryId}}
  body: none
  auth: none
}

docs {
  # API-002
  GET /api/kiosk/menuList?categoryId=
  MENU_API_CONTRACT.md
  DB view: vw_menu_list (+ vw_menu_availability)
  응답 필드: categoryId, isSoldOut, hasSoldOutIngredient, isOrderable, baseKcal
}
