meta {
  name: Admin Update Payment Method
  type: http
  seq: 16
}

patch {
  url: {{baseUrl}}/api/admin/paymentMethods/{{methodId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "active": true,
    "sortOrder": 1,
    "receiptMessage": null
  }
}

docs {
  # API-016
  PATCH /api/admin/paymentMethods/{methodId}
  body: active, sortOrder?, receiptMessage?
  키오스크 API-014 와 동일하게 active 사용. 구 isEnabled / isActive 폐기.
  SPEC_ONLY: AdminPaymentMethodController 클래스 매핑만 있음. 메서드 없음 (TODO-011).
}
