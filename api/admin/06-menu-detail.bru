meta {
  name: Admin Menu Detail
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/admin/menus/{{menuId}}
  body: none
  auth: none
}

docs {
  # API-023
  GET /api/admin/menus/{menuId}
  응답 code: ADMIN_MENU_DETAIL_SUCCESS
  없으면 MENU_NOT_FOUND
}
