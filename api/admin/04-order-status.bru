meta {
  name: Admin Change Order Status
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/admin/orders/{{orderId}}/status
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "status": "COMPLETED"
  }
}

docs {
  # API-008
  PATCH /api/admin/orders/{orderId}/status
  ORDER_API_CONTRACT: body.status
  enum: RECEIVED | PREPARING | COMPLETED | CANCELED
}
