meta {
  name: Admin Daily Sales
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/api/admin/sales/daily?date={{salesDate}}&intervalMinutes={{intervalMinutes}}
  body: none
  auth: none
}

docs {
  # API-017
  GET /api/admin/sales/daily?date
  SPEC_ONLY: AdminStatsController 클래스 매핑만 있음. 메서드 없음 (TODO-017).
}
