meta {
  name: Admin Update Payment Method
  type: http
  seq: 12
}

patch {
  url: {{baseUrl}}/api/admin/paymentMethods/{{methodId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "isActive": true,
    "sortOrder": 1,
    "receiptMessage": null
  }
}

docs {
  # API-016
  PATCH /api/admin/paymentMethods/{methodId}
  body: isActive, sortOrder?, receiptMessage?
  FE PaymentMethod draft 필드와 동일. (구 isEnabled 사용 금지)
}
