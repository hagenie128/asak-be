meta {
  name: Admin Update Payment Method
  type: http
  seq: 12
}

patch {
  url: {{baseUrl}}/api/admin/payment-methods/{{methodId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "isEnabled": true
  }
}

docs {
  # API-016
  PATCH /api/admin/payment-methods/{methodId}
  활성 토글. CARD=카드·삼성페이
}
