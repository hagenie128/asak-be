meta {
  name: Admin Menu List
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/admin/menus?categoryId={{categoryId}}&keyword=&isSoldOut=false&tagId=&page=0&size=20&sort=name,asc
  body: none
  auth: none
}

docs {
  # API-011
  GET /api/admin/menus
  MENU_API_CONTRACT.md — categoryId, isSoldOut (isActive 없음)
}
