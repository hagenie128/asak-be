meta {
  name: Admin Order Detail
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/admin/orders/{{orderId}}
  body: none
  auth: none
}

docs {
  # API-022
  GET /api/admin/orders/{orderId}
  응답 code: ADMIN_ORDER_DETAIL_SUCCESS
  없으면 ORDER_NOT_FOUND
  data: orderId, orderNo, orderType, orderStatus, paymentStatus, paymentMethod, totalAmount, createdAt, items[]
}
