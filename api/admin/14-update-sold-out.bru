meta {
  name: Admin Update Sold Out
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/api/admin/soldOut
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "changes": [
      {
        "targetType": "INGREDIENT",
        "targetId": 33,
        "isSoldOut": true
      }
    ]
  }
}

docs {
  # API-009
  PATCH /api/admin/soldOut
  body: { changes: [{ targetType, targetId, isSoldOut }] }
  targetType: MENU | INGREDIENT | OPTION_ITEM
  SPEC_ONLY: AdminSoldOutController 클래스 매핑만 있음. 메서드 없음 (TODO-007).
}
